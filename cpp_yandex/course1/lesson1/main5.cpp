/*
#include <iostream>

using namespace std;

int main()
{
	double n, a, b, x, y, res;

	cin >> n >> a >> b >> x >> y;

	if (n > b)
		res = n * (100 - y) / 100;
	else if (n > a)
		res = n * (100 - x) / 100;
	else
		res = n;

	cout << res << endl;

	return 0;
}
*/
